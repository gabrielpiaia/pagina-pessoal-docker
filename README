inicializar o container:(dentro do diretório)
docker-compose up -d --build


parar container:(dentro do diretório)
docker-compose down

verificar container se o container está up:
docker ps


verificar logs do container:
docker logs --follow nome_do_container

